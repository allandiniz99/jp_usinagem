
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug React App (Vite + Task)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:3000/",
      "webRoot": "${workspaceFolder}/src",
      "sourceMaps": true,
      "preLaunchTask": "vite: dev"
    }
  ]
}
